@model IEnumerable<SIRERH.Models.TECNOLOGIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TECNOLOGIA1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TECNOLOGIA1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TECNOLOGIA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TECNOLOGIA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TECNOLOGIA })
        </td>
    </tr>
}

</table>
